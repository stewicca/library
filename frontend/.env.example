# Base URL the SPA calls. In dev, leave as /api/v1 so Vite's proxy forwards to the backend
# (keeping the browser same-origin, which keeps the refresh-token cookie working).
# In production, point this at your API gateway, e.g. https://api.example.com/api/v1
VITE_API_BASE_URL=/api/v1
